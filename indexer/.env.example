# Postgres connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fundraising

# Port for the webhook server
PORT=4001

# Optional: require auth on POST /chainhook.
# Note: Hiro-hosted chainhooks (via @hirosystems/chainhooks-client) do NOT support
# custom headers on the outgoing http_post, so you cannot send Authorization.
# If you use the hosted Chainhooks API, leave this unset.
# CHAINHOOK_AUTH_TOKEN=replace-me

# Optional: if set, only accept/parse prints from this contract
# Example: SPXXXX....fundraising
EXPECTED_CONTRACT_IDENTIFIER=SP3R3SX667CWE61113X23CAQ03SZXXZ3D8D3A4NFH.fundraising

# Hiro-hosted Chainhooks API registration (mainnet/testnet only)
# You must expose a PUBLIC webhook URL that Hiro can reach.
# For local development, use a tunnel (ngrok/cloudflared) and set CHAINHOOKS_WEBHOOK_URL.
CHAINHOOKS_NETWORK=mainnet
CHAINHOOKS_API_KEY=replace-me
# Alternative auth:
# CHAINHOOKS_JWT=replace-me

# Optional override; defaults to https://api.<network>.hiro.so
# CHAINHOOKS_BASE_URL=https://api.mainnet.hiro.so

# Where Hiro will POST events
# CHAINHOOKS_WEBHOOK_URL=https://your-public-domain.com/chainhook

# Optional: chainhook name (used for replace behavior)
# CHAINHOOKS_NAME=Fundraising contract logs

# Optional: if true, deletes an existing chainhook with the same name+network first
# CHAINHOOKS_REPLACE_EXISTING=true
